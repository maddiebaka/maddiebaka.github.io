# Welcome to Jekyll!
#
# This config file is meant for settings that affect your whole blog, values
# which you are expected to set up once and rarely edit after that. If you find
# yourself editing this file very often, consider using Jekyll's data files
# feature for the data you need to update frequently.
#
# For technical reasons, this file is *NOT* reloaded automatically when you use
# 'bundle exec jekyll serve'. If you change this file, please restart the server process.
#
# If you need help with YAML syntax, here are some quick references for you:
# https://learn-the-web.algonquindesign.ca/topics/markdown-yaml-cheat-sheet/#yaml
# https://learnxinyminutes.com/docs/yaml/
#
# Site settings
# These are used to personalize your new site. If you look in the HTML files,
# you will see them accessed via {{ site.title }}, {{ site.email }}, and so on.
# You can create any custom variable you would like, and they will be accessible
# in the templates via {{ site.myvariable }}.

title: '@maddiefuzz'
email: maddiedev@riseup.net
description: >- # this means to ignore newlines until "baseurl:"
  Technical writing and musing from the void.  
baseurl: "" # the subpath of your site, e.g. /blog
url: "https://maddie.info" # the base hostname & protocol for your site, e.g. http://example.com
twitter_username: jekyllrb
github_username: maddiebaka
repository: maddiebaka/maddiebaka.github.io
footer:
  links:
    - label: "Ko-Fi"
      url: "https://www.ko-fi.com/maddiefuzz"

# Build settings
#theme: minimal-mistakes-jekyll
remote_theme: mmistakes/minimal-mistakes
minimal_mistakes_skin: sunrise
plugins:
  - jekyll-feed
  - jekyll-remote-theme
  - jekyll-include-cache

# Exclude from processing.
# The following items will not be processed, by default.
# Any item listed under the `exclude:` key here will be automatically added to
# the internal "default list".
#
# Excluded items can be processed by explicitly listing the directories or
# their entries' file path in the `include:` list.
#
# exclude:
#   - .sass-cache/
#   - .jekyll-cache/
#   - gemfiles/
#   - Gemfile
#   - Gemfile.lock
#   - node_modules/
#   - vendor/bundle/
#   - vendor/cache/
#   - vendor/gems/
#   - vendor/ruby/

# Build reproducibility (see .github/workflows/pages.yml)
#
# Pin the timezone so local builds and CI agree on post dates. GitHub's
# classic Pages builder ran in UTC, so UTC keeps every existing post URL
# exactly where it is.
timezone: "UTC"

sass:
  # GitHub's Pages builder minified this for us; Jekyll 4 defaults to
  # :expanded, which grew main.css from 70K to 107K.
  style: compressed
  # Minimal Mistakes vendors an old Susy that trips ~230 Dart Sass
  # division deprecations per build. Not ours to fix, so don't print them.
  quiet_deps: true
  # The theme's own main.scss still uses @import, removed in Dart Sass 3.
  silence_deprecations: ["import"]

exclude:
  - new_post.rb
